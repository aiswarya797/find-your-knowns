---
description: Route under-specified, high-impact, or high-risk implementation work through Find Your Knowns before coding, even when the user does not name the uncertainty.
globs:
  - "**/*"
alwaysApply: true
---

# Find Your Knowns

Use this rule when a request is vague, under-specified, design-heavy,
reference-based, high-risk, long-running, or likely to hide product, API, data,
security, or operational constraints. Do not wait for the user to say they are
uncertain; infer risk from the task shape and blast radius.

Load and follow:

```text
skills/find-your-knowns/SKILL.md
```

This rule is only a router. For tiny, direct, low-risk edits, do not load the
full workflow; proceed normally after noting that unknowns are low-impact.

Important boundaries:

- Use Find Your Knowns to discover missing context before implementation.
- Treat missing constraints, success criteria, edge cases, rollout expectations,
  or quality bar as a trigger.
- Do not use it as ordinary code review, bug review, security review, or PR
  defect finding.
- Inspect the actual repo or product surface before ranking risks.
- Ask one high-impact question at a time only when the answer would change the
  implementation.
- Produce a concrete artifact: risk table, direction set, reference map,
  implementation note, buy-in doc, or change quiz.

Useful prompts:

```text
Use Find Your Knowns before planning this feature.
```

```text
Run a blindspot pass on this module before implementation.
```

```text
Show design directions before wiring the real UI.
```
